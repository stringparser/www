import Scale from '../../shared/components/Scale';

export const meta = {
  title: 'Scale generator',
  description: 'hi',
};

# Scale

Here we can generate some scales using the following component.

<Scale />
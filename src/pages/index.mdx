import { getPostFiles } from '../shared/util';

export const frontMatter = {
  name: 'index',
};

import Box from 'superbox';

# Here you go

This place, like many things, is a work progress. Feel free go around and browse things up.

<>
  {getPostFiles()}
</>